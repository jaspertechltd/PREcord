🛠️ RE_COMBOBULATOR: Fix MainScreenViewModelTest compilation

Resolves compilation errors in unit tests reported by TESTY.

- [x] Fix compilation errors in `MainScreenViewModelTest.kt` caused by a duplicate class definition and rogue import statements.
- [x] Run local compile check (`./gradlew test` & `./gradlew assembleDebug`) to verify fix.
